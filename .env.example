# Supabase (copy to .env.local or fill src/environments/environment*.ts)
# Get these from Project Settings → API in the Supabase dashboard.
# Never commit service_role keys.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
